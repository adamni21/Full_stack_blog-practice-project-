import { FC } from "react";
import { StyledMessage } from "./ConfirmModal.styles";


const LoadingFrame: FC = () => {
  return <StyledMessage>Loading...</StyledMessage>;
};

export default LoadingFrame;
